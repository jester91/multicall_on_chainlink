# MultiCall with single API call to a infure node
## Prerequisites
```bash
pip install brownie
pip install matplotlib
```
# Run the code

```bash
brownie run scripts/multicall.py
brownie run scripts/multicall_plot.py --network mainnet
```